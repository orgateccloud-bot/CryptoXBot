# Dependências de desenvolvimento/CI (não vão para a imagem de produção).
# Instale com: pip install -r requirements-dev.txt
-r requirements.txt

# Testes
pytest>=8.0
pytest-cov>=5.0
pytest-asyncio>=0.23

# Qualidade / segurança (espelham os hooks de .pre-commit-config.yaml)
black>=24.1
flake8>=7.0
isort>=5.13
mypy>=1.8
bandit>=1.7
detect-secrets>=1.4
pre-commit>=3.6
